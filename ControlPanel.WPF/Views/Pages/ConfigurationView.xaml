<UserControl
    x:Class="ControlPanel.Presentation.WPF.Views.Pages.ConfigurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ControlPanel.Presentation.WPF.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="4">
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Text="{Binding Title}" />
            <TextBlock Margin="4" Text="Choose COM Port" />
            <ComboBox
                Margin="4"
                ItemsSource="{Binding AvailableComPorts}"
                SelectedItem="{Binding SelectedComPortName}" />
            <Button
                Width="80"
                Margin="4"
                HorizontalAlignment="Right"
                Command="{Binding GetAvailableComPortsCommand}"
                Content="Refresh" />
            <Separator Margin="0,10" />
            <TextBlock Margin="4" Text="Choose Baud Rate" />
            <ComboBox
                Margin="4"
                ItemsSource="{Binding AvailableBaudRates}"
                SelectedItem="{Binding SelectedBaudRate}" />
            <Separator Margin="0,10" />
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    Width="80"
                    Margin="5"
                    Command="{Binding ConnectCommand}"
                    Content="Connect" />
                <Button
                    Width="80"
                    Margin="5"
                    Command="{Binding DisconnectCommand}"
                    Content="Disconnect" />
            </StackPanel>

        </StackPanel>
        <!--  status  -->
        <StackPanel
            Grid.Column="1"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Separator Margin="0,10" />
            <StackPanel Margin="4" Orientation="Vertical">
                <TextBlock Margin="4" Text="Status" />
                <TextBlock
                    Margin="4"
                    Foreground="{Binding ConnectionStatusColor}"
                    Text="{Binding ConnectionStatus}" />
                <Separator Margin="0,10" />
                <TextBlock Margin="4" Text="Connection Log" />
                <TextBox
                    MinWidth="400"
                    MaxHeight="420"
                    Margin="4"
                    HorizontalScrollBarVisibility="Auto"
                    IsReadOnly="True"
                    Text="{Binding CommunicationLog}"
                    TextChanged="TextBox_TextChanged"
                    VerticalScrollBarVisibility="Visible" />
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
